# API Configuration
# Live backend base URL. Leave empty to use seeded fallback data.
VITE_API_URI=http://localhost:3000

# Set true to force seeded fallback mode even when VITE_API_URI is configured.
VITE_DEMO_MODE=false
